/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.hashnode.com'],
        unoptimized: true
    },
    reactStrictMode: true
}

module.exports = nextConfig
